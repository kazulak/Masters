#ifndef PARAM_H
#define PARAM_H

#include <stdint.h>

// Define the data type
typedef uint32_t T;

// Constants used in the host and kernel
static const uint64_t nr_elements = 1024;
static const uint32_t dpu_number = 1;
static const int print_info = 1;

#endif
