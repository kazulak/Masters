schema_version: tn_benchmark_v3
experiment_id: external-tn-context-stress18
defaults:
  timeout_s: 120.0
collection:
  claim_policy: diagnostic_v1
  base_seed: 20260828
  warmup_blocks: 1
  measurement_blocks: 5
  session_policy: fresh_session_per_attempt_v1
  block_cooldown_s: 0.0
  machine_policy:
    machine_exclusivity:
      mode: observed_v1
    cpu_governor:
      mode: observed_v1
    affinity:
      mode: observed_v1
      expected_cpus: null
    numa_policy:
      mode: observed_v1
    background_load:
      mode: observed_v1
      max_load1_per_online_cpu: null
cases:
  stress18:
    circuit:
      kind: builtin
      name: quantization_stress
      path: null
      parameters:
        n_qubits: 18
        repeat_layers: 2
plans: {}
routes:
  quimb_greedy:
    executor: quimb
    numeric_policy: null
    options:
      optimize: greedy
  quimb_cotengra_path:
    executor: cotengra
    numeric_policy: null
    options:
      methods: greedy
      max_repeats: 1
matrix:
  - case_id: stress18
    plan_id: null
    route_ids: [quimb_greedy, quimb_cotengra_path]
