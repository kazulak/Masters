diff -U0 -r a/lib/processing/ProcessingHelperHost.c b/lib/processing/ProcessingHelperHost.c
--- a/lib/processing/ProcessingHelperHost.c
+++ b/lib/processing/ProcessingHelperHost.c
@@ -4 +4,4 @@
-    handle_t* handle = malloc(sizeof(handle_t));
+    handle_t* handle = calloc(1, sizeof(handle_t));
+    if(handle == NULL){
+        return NULL;
+    }
@@ -6,4 +9,11 @@
-    handle->bin_location = malloc(2048);
-    char func_bodyname[2048];
-    strcpy(func_bodyname, func_pathname);
-    for(int i=0; i<2048; i++){
+    handle->bin_location = calloc(1, 2048);
+    if(handle->bin_location == NULL){
+        goto failure;
+    }
+    char func_bodyname[2048] = {0};
+    size_t func_bodyname_len = strlen(func_pathname);
+    if(func_bodyname_len >= sizeof(func_bodyname)){
+        goto failure;
+    }
+    memcpy(func_bodyname, func_pathname, func_bodyname_len + 1);
+    for(size_t i=0; i<func_bodyname_len; i++){
@@ -21,0 +32,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -35,0 +49,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -64,0 +81,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -71,0 +91,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -79,0 +102,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -85,2 +110,8 @@
-
-        handle->so_bin_location = malloc(2048);
+        if(succ != 0){
+            goto failure;
+        }
+
+        handle->so_bin_location = calloc(1, 2048);
+        if(handle->so_bin_location == NULL){
+            goto failure;
+        }
@@ -97,0 +129,3 @@
+        if(succ != 0){
+            goto failure;
+        }
@@ -105 +139 @@
-        return NULL;
+        goto failure;
@@ -109,0 +144,5 @@
+failure:
+    free(handle->so_bin_location);
+    free(handle->bin_location);
+    free(handle);
+    return NULL;
diff -U0 -r a/lib/processing/gen_red/GenRed.c b/lib/processing/gen_red/GenRed.c
--- a/lib/processing/gen_red/GenRed.c
+++ b/lib/processing/gen_red/GenRed.c
@@ -89,0 +90,3 @@
+    if(binary_handle == NULL || binary_handle->so_bin_location == NULL){
+        return NULL;
+    }
@@ -150,0 +154,3 @@
+        if(my_table == NULL){
+            return NULL;
+        }
@@ -152,0 +159,6 @@
+        if(lib == NULL){
+            printf("dynamic library linking failed!!!\n");
+            free(my_table);
+            return NULL;
+        }
+        dlerror();
@@ -153,0 +166,2 @@
+        const char* init_error = dlerror();
+        dlerror();
@@ -154,0 +169 @@
+        const char* combine_error = dlerror();
@@ -156,2 +171,5 @@
-        if(lib == NULL){
-        printf("dynamic library linking failed!!!\n");
+        if(init_error != NULL || combine_error != NULL || init_func == NULL || combine_func == NULL){
+            printf("dynamic reduction callback lookup failed!!!\n");
+            dlclose(lib);
+            free(my_table);
+            return NULL;
@@ -209,0 +228 @@
+    return NULL;
diff -U0 -r a/lib/processing/map/Map.c b/lib/processing/map/Map.c
--- a/lib/processing/map/Map.c
+++ b/lib/processing/map/Map.c
@@ -157,0 +158 @@
+        table_management->free_space_start_pos = table_management->free_space_start_pos > t->end ? table_management->free_space_start_pos : t->end;
diff -U0 -r a/lib/processing/map/MapProcessing.h b/lib/processing/map/MapProcessing.h
--- a/lib/processing/map/MapProcessing.h
+++ b/lib/processing/map/MapProcessing.h
@@ -40 +40 @@
-    uint32_t unroll_block_rest = copy_block_size-unroll_block_rest;
+    uint32_t unroll_block_rest = copy_block_size-unroll_block_size;
@@ -172 +172 @@
-    uint32_t unroll_block_rest = copy_block_size-unroll_block_rest;
+    uint32_t unroll_block_rest = copy_block_size-unroll_block_size;
