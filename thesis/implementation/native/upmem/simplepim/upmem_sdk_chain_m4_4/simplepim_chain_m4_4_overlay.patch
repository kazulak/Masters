diff -ruN a/lib/communication/CommOps.c b/lib/communication/CommOps.c
--- a/lib/communication/CommOps.c	2026-08-09 20:21:59.693355409 +0200
+++ b/lib/communication/CommOps.c	2026-08-09 20:21:59.699355336 +0200
@@ -39,7 +39,7 @@
     }
     // elements must be 8 bytes aligned and divisible by num_dpus， pad not aglined, use malloc_aligned
     if(contains_table(table_id, table_management)){
-        printf(table_id);
+        printf("%s", table_id);
         printf(" is contained in table management unit, invalid scatter\n");
         return;
     }
@@ -96,7 +96,7 @@

 void* simplepim_gather(char* const table_id, simplepim_management_t* table_management){
     if(!contains_table(table_id, table_management)){
-        printf(table_id);
+        printf("%s", table_id);
         printf(" is not contained in table management unit, invalid scatter\n");
         return NULL;
     }
@@ -175,7 +175,7 @@

 void simplepim_allgather(char* const table_id, char* const new_table_id, simplepim_management_t* table_management){
     if(!contains_table(table_id, table_management)){
-        printf(table_id);
+        printf("%s", table_id);
         printf(" is not contained in table management unit, invalid allgather\n");
         return;
     }
@@ -206,7 +206,7 @@
     if(binary_handle->func_type == 1){
         if(!contains_table(table_id, table_management)){
             printf("source table ");
-            printf(table_id);
+            printf("%s", table_id);
             printf(" is not contains in current management unit\n");
             return;
         }
@@ -249,7 +249,7 @@
     }
     else{
         printf("ERROR: compiled binary ");
-        printf(binary_handle->bin_location);
+        printf("%s", binary_handle->bin_location);
         printf(" does not contain general reduction functions\n");
     }

diff -ruN a/lib/management/Management.c b/lib/management/Management.c
--- a/lib/management/Management.c	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/management/Management.c	2026-08-09 20:21:59.699355336 +0200
@@ -7,7 +7,7 @@
 simplepim_management_t* table_management_init(uint32_t num_dpus){

     struct dpu_set_t set;
-    DPU_ASSERT(dpu_alloc(num_dpus, NULL, &set));
+    DPU_ASSERT(dpu_alloc(num_dpus, "backend=hw", &set));

     small_table_init(set);
     simplepim_management_t* management = malloc(sizeof(simplepim_management_t));
@@ -75,7 +75,7 @@
     }

     printf("table ");
-    printf(name);
+    printf("%s", name);
     printf(" is not contains in current management unit\n");
     return NULL;
 }

diff -ruN a/lib/processing/map/MapProcessing.h b/lib/processing/map/MapProcessing.h
--- a/lib/processing/map/MapProcessing.h	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/map/MapProcessing.h	2026-08-09 20:21:59.699355336 +0200
@@ -243,1 +243,1 @@
-                for(int k=input_type_1_rest_4; k<input_type1; k++){
+                for(int k=(input_type_1_div_4 << 2); k<input_type1; k++){
@@ -252,1 +252,1 @@
-                for(int k=input_type_2_rest_4; k<input_type2; k++){
+                for(int k=(input_type_2_div_4 << 2); k<input_type2; k++){
@@ -293,1 +293,1 @@
-            for(int k=input_type_1_rest_4; k<input_type1; k++){
+            for(int k=(input_type_1_div_4 << 2); k<input_type1; k++){
@@ -302,1 +302,1 @@
-            for(int k=input_type_2_rest_4; k<input_type2; k++){
+            for(int k=(input_type_2_div_4 << 2); k<input_type2; k++){
diff -ruN a/lib/processing/ProcessingHelperHost.c b/lib/processing/ProcessingHelperHost.c
--- a/lib/processing/ProcessingHelperHost.c	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/ProcessingHelperHost.c	2026-08-09 20:21:59.720355083 +0200
@@ -1,31 +1,49 @@
 #include "ProcessingHelperHost.h"
+#ifndef THESIS_SIMPLEPIM_OPERATOR_TASKLETS
+#define THESIS_SIMPLEPIM_OPERATOR_TASKLETS 1
+#endif
+#define THESIS_SIMPLEPIM_STRINGIFY_IMPL(value) #value
+#define THESIS_SIMPLEPIM_STRINGIFY(value) THESIS_SIMPLEPIM_STRINGIFY_IMPL(value)
 handle_t* create_handle(const char* func_pathname, uint32_t func_type){

-    handle_t* handle = malloc(sizeof(handle_t));
+    handle_t* handle = calloc(1, sizeof(handle_t));
+    if(handle == NULL){
+        return NULL;
+    }
     handle->func_type = func_type;
-    handle->bin_location = malloc(2048);
-    char func_bodyname[2048];
-    strcpy(func_bodyname, func_pathname);
-    for(int i=0; i<2048; i++){
+    handle->bin_location = calloc(1, 2048);
+    if(handle->bin_location == NULL){
+        goto failure;
+    }
+    char func_bodyname[2048] = {0};
+    size_t func_bodyname_len = strlen(func_pathname);
+    if(func_bodyname_len >= sizeof(func_bodyname)){
+        goto failure;
+    }
+    memcpy(func_bodyname, func_pathname, func_bodyname_len + 1);
+    for(size_t i=0; i<func_bodyname_len; i++){
             if(func_bodyname[i] == '.'){
                 func_bodyname[i] = '_';
             }
     }

     if(func_type == 0){
-        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=12 -I./ -D__mapfunc_filename__=\"<";
+        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=" THESIS_SIMPLEPIM_STRINGIFY(THESIS_SIMPLEPIM_OPERATOR_TASKLETS) " -I./ -D__mapfunc_filename__=\"<";
         strcat(compile_cmd, func_pathname);
         strcat(compile_cmd, "/map.h>\" -o bin/dpu_map_");
         strcat(compile_cmd, func_bodyname);
         strcat(compile_cmd, " ../../lib/processing/map/map_dpu.c ../../lib/processing/ProcessingHelper.c");
         int succ = system(compile_cmd);
+        if(succ != 0){
+            goto failure;
+        }

         char bin_location[2048] = "bin/dpu_map_";
         strcat(bin_location, func_bodyname);
         strcpy(handle->bin_location, bin_location);
     }
     else if(func_type == 1){
-        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=12 -I./ -D__mapredfunc_pathname__=\"<";
+        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=" THESIS_SIMPLEPIM_STRINGIFY(THESIS_SIMPLEPIM_OPERATOR_TASKLETS) " -I./ -D__mapredfunc_pathname__=\"<";
         strcat(compile_cmd, func_pathname);
         strcat(compile_cmd, "/map_to_val_func.h>\" -D__combinefunc_pathname__=\"<");
         strcat(compile_cmd, func_pathname);
@@ -33,6 +51,9 @@
         strcat(compile_cmd, func_bodyname);
         strcat(compile_cmd, " ../../lib/processing/gen_red/gen_red_dpu.c ../../lib/processing/ProcessingHelper.c ../../lib/TableHost.c ../../lib/Table.c ../../lib/TableShared.c");
         int succ = system(compile_cmd);
+        if(succ != 0){
+            goto failure;
+        }

         char bin_location[2048] = "bin/dpu_genred_";
         strcat(bin_location, func_bodyname);
@@ -62,6 +83,9 @@
         strcat(cp_cmd, " ");
         strcat(cp_cmd, c_fname);
         succ = system(cp_cmd);
+        if(succ != 0){
+            goto failure;
+        }

         char compile_cmd1[2048] = "gcc -c -fPIC -o";
         strcat(compile_cmd1, " bin/");
@@ -69,6 +93,9 @@
         strcat(compile_cmd1, " ");
         strcat(compile_cmd1, c_fname);
         succ = system(compile_cmd1);
+        if(succ != 0){
+            goto failure;
+        }


         char compile_cmd2[2048] = "gcc -shared -o bin/";
@@ -77,13 +104,22 @@
         strcat(compile_cmd2, " bin/");
         strcat(compile_cmd2, o_fname);
         succ = system(compile_cmd2);
+        if(succ != 0){
+            goto failure;
+        }


         char compile_cmd3[2048] = "rm ";
         strcat(compile_cmd3, c_fname);
         succ = system(compile_cmd3);
-
-        handle->so_bin_location = malloc(2048);
+        if(succ != 0){
+            goto failure;
+        }
+
+        handle->so_bin_location = calloc(1, 2048);
+        if(handle->so_bin_location == NULL){
+            goto failure;
+        }
         char so_bin_location[2048] = "bin/";
         strcat(so_bin_location, so_fname);
         strcpy(handle->so_bin_location, so_bin_location);
@@ -92,9 +128,12 @@

     }
     else if(func_type == 2){
-        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=12 -o bin/dpu_zip";
+        char compile_cmd[2048] = "dpu-upmem-dpurte-clang -O2 -DNR_TASKLETS=" THESIS_SIMPLEPIM_STRINGIFY(THESIS_SIMPLEPIM_OPERATOR_TASKLETS) " -o bin/dpu_zip";
         strcat(compile_cmd, " ../../lib/processing/zip/zip_dpu.c ../../lib/processing/zip/ZipProcessing.c ../../lib/processing/ProcessingHelper.c");
         int succ = system(compile_cmd);
+        if(succ != 0){
+            goto failure;
+        }

         char bin_location[2048] = "bin/dpu_zip";
         strcpy(handle->bin_location, bin_location);
@@ -102,9 +141,14 @@
     }
     else{
         printf("function handle not properly compiled!!!");
-        return NULL;
+        goto failure;
     }

     return handle;

+failure:
+    free(handle->so_bin_location);
+    free(handle->bin_location);
+    free(handle);
+    return NULL;
 }
diff -ruN a/lib/processing/gen_red/GenRed.c b/lib/processing/gen_red/GenRed.c
--- a/lib/processing/gen_red/GenRed.c	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/gen_red/GenRed.c	2026-08-09 20:21:59.706355252 +0200
@@ -87,6 +87,9 @@
     struct timeval start_time;
     struct timeval end_time;
     uint32_t outputs = table_management->free_space_start_pos;
+    if(binary_handle == NULL || binary_handle->so_bin_location == NULL){
+        return NULL;
+    }
     if(contains_table(dest_name, table_management)){
         outputs = lookup_table(dest_name, table_management) -> start;
     }
@@ -94,7 +97,7 @@
     if(binary_handle->func_type == 1){
         if(!contains_table(src_name, table_management)){
             printf("source table ");
-            printf(src_name);
+            printf("%s", src_name);
             printf(" is not contains in current management unit\n");
             return NULL;
         }
@@ -148,13 +151,28 @@
         // reduction on cpu
         gettimeofday(&start_time, NULL);
         void* my_table = malloc(output_len*output_type);
+        if(my_table == NULL){
+            return NULL;
+        }

         void* lib=dlopen(binary_handle->so_bin_location, RTLD_NOW);
+        if(lib == NULL){
+            printf("dynamic library linking failed!!!\n");
+            free(my_table);
+            return NULL;
+        }
+        dlerror();
         void (*init_func)(uint32_t, void*) = dlsym(lib,"init_func");
+        const char* init_error = dlerror();
+        dlerror();
         void (*combine_func)(void*, void*) = dlsym(lib, "combine_func");
+        const char* combine_error = dlerror();

-        if(lib == NULL){
-        printf("dynamic library linking failed!!!\n");
+        if(init_error != NULL || combine_error != NULL || init_func == NULL || combine_func == NULL){
+            printf("dynamic reduction callback lookup failed!!!\n");
+            dlclose(lib);
+            free(my_table);
+            return NULL;
         }


@@ -193,7 +211,7 @@

         printf("--------------\n");
         printf("table reduction function : ");
-        printf(binary);
+        printf("%s", binary);
         printf("\nreduction function kernel execution time : %f\n", kernel_time/1000);
         printf("host reduction execution time : %f\n", host_table_reduction_time/1000);
         printf("function call and table management time : %f\n", (register_table_time+prepare_args_time)/1000);
@@ -203,8 +221,9 @@
     }
     else{
         printf("ERROR: compiled binary ");
-        printf(binary_handle->bin_location);
+        printf("%s", binary_handle->bin_location);
         printf(" does not contain general reduction functions\n");
     }

+    return NULL;
 }
diff -ruN a/lib/processing/map/Map.c b/lib/processing/map/Map.c
--- a/lib/processing/map/Map.c	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/map/Map.c	2026-08-09 20:21:59.699355336 +0200
@@ -13,7 +13,7 @@

     if(!contains_table(src_name, table_management)){
             printf("source table ");
-            printf(src_name);
+            printf("%s", src_name);
             printf(" is not contains in current management unit\n");
             return;
     }
@@ -86,7 +86,7 @@

         printf("--------------\n");
         printf("map function : ");
-        printf(binary);
+        printf("%s", binary);
         printf("\nmap function kernel execution time : %f\n", kernel_time/1000);
         printf("function call and table management time : %f\n", (register_table_time+prepare_args_time)/1000);
         printf("--------------\n");
@@ -155,6 +155,7 @@

         add_table(t, table_management);

+        table_management->free_space_start_pos = table_management->free_space_start_pos > t->end ? table_management->free_space_start_pos : t->end;

         gettimeofday(&end_time, NULL);
         double register_table_time = (end_time.tv_sec - start_time.tv_sec) * 1000000.0 +
@@ -162,14 +163,14 @@

         printf("--------------\n");
         printf("map function : ");
-        printf(binary);
+        printf("%s", binary);
         printf("\nmap function kernel execution time : %f\n", kernel_time/1000);
         printf("function call and table management time : %f\n", (register_table_time+prepare_args_time)/1000);
         printf("--------------\n");
     }
     else{
         printf("ERROR: compiled binary ");
-        printf(binary_handle->bin_location);
+        printf("%s", binary_handle->bin_location);
         printf(" does not contain map function\n");
     }

diff -ruN a/lib/processing/map/MapProcessing.h b/lib/processing/map/MapProcessing.h
--- a/lib/processing/map/MapProcessing.h	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/map/MapProcessing.h	2026-08-09 20:21:59.699355336 +0200
@@ -37,7 +37,7 @@


     uint32_t unroll_block_size = (copy_block_size>>2)<<2;
-    uint32_t unroll_block_rest = copy_block_size-unroll_block_rest;
+    uint32_t unroll_block_rest = copy_block_size-unroll_block_size;

     uint32_t i_init = pid<<copy_block_size_shiftbits;
     uint32_t i_elem = (uint32_t)inputs+i_init*elem_type_size;
@@ -169,7 +169,7 @@
     void* elem_plus_input1 = elem + input_type1;

     uint32_t unroll_block_size = (copy_block_size>>2)<<2;
-    uint32_t unroll_block_rest = copy_block_size-unroll_block_rest;
+    uint32_t unroll_block_rest = copy_block_size-unroll_block_size;

     if(input_type1 ==4 && input_type2 == 4){
         for(int i=pid*copy_block_size; i<divisible_len; i+=block_times_tasklets){
diff -ruN a/lib/processing/zip/Zip.c b/lib/processing/zip/Zip.c
--- a/lib/processing/zip/Zip.c	2026-08-09 20:21:59.694355397 +0200
+++ b/lib/processing/zip/Zip.c	2026-08-09 20:21:59.706355252 +0200
@@ -19,14 +19,14 @@

         if(!contains_table(src1_name, table_management)){
             printf("source table ");
-            printf(src1_name);
+            printf("%s", src1_name);
             printf(" is not contains in current management unit\n");
             return;
         }

         if(!contains_table(src2_name, table_management)){
             printf("source table ");
-            printf(src2_name);
+            printf("%s", src2_name);
             printf(" is not contains in current management unit\n");
             return;
         }
@@ -155,7 +155,7 @@
     }
     else{
         printf("ERROR: compiled binary ");
-        printf(binary_handle->bin_location);
+        printf("%s", binary_handle->bin_location);
         printf(" is not a zip function\n");
     }
 }
